This is the frontend part of task calendar application using AngularJS.

* Deployment instructions
- npm install
- http-server -p 8000 -o (to open local server)

* you need to have backend server running before this.
